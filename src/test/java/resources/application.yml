#jpa
spring.jpa:
  show-sql: true
  hibernate:
    ddl-auto: update
  properties:
    hibernate:

# data 소스
spring.datasource:
  url: jdbc:h2:mem:testdb
  driver-class-name: org.h2.Driver
  username: sa
  password: password

# 테스트용 h2
spring.h2.console:
  enabled: true
  path: /h2-console